{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "npm install -g @angular/cli && npm install && npm run build"
        }
      }
    ]
  }
  